# App-scoped dependencies (kept separate from evals/requirements.txt).
anthropic>=0.40         # agent model (claude-sonnet-4-6), streaming + tool use
fastapi>=0.111          # SSE service
uvicorn[standard]>=0.30 # ASGI server
openai>=1.40            # query embedding (text-embedding-3-large@1536)
asyncpg>=0.29           # Postgres pool for kb_faq hybrid RRF
httpx>=0.27             # FinX MIS calls (CHO-21)
python-dotenv>=1.0      # load repo .env
# dev / test
pytest>=8.0
pytest-asyncio>=0.23
